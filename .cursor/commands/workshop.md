# /workshop

文枢创意工坊。请在项目根执行完整写稿流程，禁止在对话里直接代写正文。

解析用户在斜杠命令后输入的需求，运行：

```bash
npx tsx scripts/workshop-cli.ts --topic "..."
# 或 --random-hot
```

完成后汇报 `output/article/` 下的 IR 与各平台变体路径。详见 `.cursor/skills/workshop/SKILL.md`。
